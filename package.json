{
  "name": "vertex-mock",
  "version": "1.0.0",
  "private": true,
  "description": "Vertex fitness battery — boxing-skill workshop mock + live audio-detected tests",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  },
  "engines": {
    "node": ">=20"
  }
}
